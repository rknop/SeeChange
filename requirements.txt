# This list is copied into the docker file
alembic==1.12.1
astro-datalab==2.20.1
astropy==5.3.4
astroquery==0.4.6
beautifulsoup4==4.12.2
fitsio==0.9.12
flaky==3.8.1
flask==3.0.3
flask-session==0.8.0
GitPython==3.1.40
gunicorn==22.0.0
h5py==3.10.0
healpy==1.16.6
matplotlib==3.8.2
numpy==1.26.2
pandas==2.1.3
photutils==1.9.0
psutil==5.9.8
psycopg2==2.9.9
pycryptodome==3.20.0
pytest==7.4.3
pytest-timestamper==0.0.10
python-dateutil==2.8.2
pytz==2022.7.1
PyYAML==6.0.1
requests==2.31.0
scipy==1.11.4
scikit-image==0.22.0
sep==1.2.1
Sphinx==7.2.6
SQLAlchemy==2.0.23
SQLAlchemy-Utils==0.41.1
tables==3.9.1
urllib3==2.1.0
wget==3.2

# torch==2.3.1
# NOTE -- pytorch is installed manually
# By default, this installs the GPU version,
#  which bloats the dockerfile from 2GB to 7GB (!).
#  (I think it's installing for lots of GPU architectures;
#  if we ever want to actually try to use the GPU pytorch,
#  then we need to figure out how to build pytorch for
#  just the architectures we need.)
#
# Following https://pytorch.org/get-started/locally/
# in the dockerfile is a command:
#   pip install torch==2.3.1 torchvision==0.18.1 --index-url https://download.pytorch.org/whl/cpu

####
# Modules below were added so that we could install RBbot, which was
# necessary to use Nabeel's pytorch R/B model.  If we find a way to load
# that pytorch model without RBbot installed, we can remove what's
# below.  We would really like to do this, because it bloats the
# Dockerfile by nearly 2GB

keras==3.4.1
penquins==2.4.0
scikit_learn==1.5.1
setuptools==69.5.1
tqdm==4.66.4

# tensorflow is installed manually to install a CPU-only version
#  pip install https://storage.googleapis.com/tensorflow/versions/2.16.1/tensorflow_cpu-2.16.1-cp311-cp311-manylinux_2_17_x86_64.manylinux2014_x86_64.whl
